---
id: {{SUBSYSTEM_ID}}_basic-design
title: 基本設計書 — {{SUBSYSTEM_NAME}}
version: 0.1
status: draft
extends:
  - ./{{SUBSYSTEM_NAME}}-requirements.md
  - {{EXTENDS_DESIGN_PATH}}
supersedes: []
superseded_by: []
related: []
---

<!-- フロントマター仕様: .claude/skills/_shared/references/doc-reference-syntax.md
     ライフサイクル規則:  .claude/skills/_shared/references/doc-lifecycle.md -->

# 基本設計書

| 項目 | 内容 |
| --- | --- |
| プロジェクト名 | |
| 文書番号 | |
| バージョン | 0.1 |
| 作成日 | YYYY-MM-DD |
| 作成者 | |
| 承認者 | |

## 改訂履歴
| 版 | 日付 | 変更者 | 変更内容 |
| --- | --- | --- | --- |
| 0.1 | YYYY-MM-DD | | 初版作成 |

---

## 1. はじめに
### 1.1 目的
### 1.2 適用範囲
### 1.3 用語定義
| 用語 | 定義 |
| --- | --- |

### 1.4 関連文書
| 文書名 | 版 | 備考 |
| --- | --- | --- |
| 要件定義書 | | |

---

## 2. システム全体概要
### 2.1 システム概要
### 2.2 システム構成図
（論理構成図 / 物理構成図を添付）

### 2.3 システム構成要素一覧
| 要素 | 種別 | 概要 |
| --- | --- | --- |

### 2.4 方式設計
- アーキテクチャ方式：
- 開発言語・フレームワーク：
- ミドルウェア：
- 採用技術・標準：

---

## 3. 業務設計
### 3.1 業務フロー（システム化後）
### 3.2 業務機能一覧
| 業務ID | 業務名 | 関連機能ID |
| --- | --- | --- |

### 3.3 業務ルール
| ルールID | 内容 |
| --- | --- |

---

## 4. アプリケーション機能設計
### 4.1 機能一覧
設計 ID は `DES-{{SUBSYSTEM}}-<n>` 形式（`{{SUBSYSTEM}}` はサブシステム名の大文字形。`_shared/references/id-conventions.md` 準拠）。要件ID 列には対応する `REQ-{{SUBSYSTEM}}-<n>` を必ず記載し、REQ-ID → DES-ID のトレーサビリティを成立させる。

| DES-ID | 機能名 | 種別 (画面/バッチ/API) | 概要 | 要件ID (REQ-ID) |
| --- | --- | --- | --- | --- |
| DES-{{SUBSYSTEM}}-1 | | | | REQ-{{SUBSYSTEM}}-1 |

### 4.2 機能関連図
### 4.3 機能詳細
#### DES-{{SUBSYSTEM}}-1: 機能名
- 対応要件：REQ-{{SUBSYSTEM}}-1（要件定義書 §5.2 の該当見出しへ相対リンクを張ることを推奨）
- 概要：
- 入力：
- 処理概要：
- 出力：
- 業務ルール：
- 例外処理：

---

## 5. 画面設計

> **方針：** Markdown はピクセル位置の描写に向かない。本章は **画面の構成要素 (components) と振る舞い (behaviors)** を文章で確定させることに集中し、レイアウト図は付録 C のモックアップ／ワイヤーフレーム集に外出しする。

### 5.1 画面一覧
| 画面ID | 画面名 | 概要 | 利用者ロール | 対応機能ID |
| --- | --- | --- | --- | --- |

### 5.2 画面遷移図
（mermaid `stateDiagram-v2` または `flowchart` で正常系・例外系をまとめて描く）

### 5.3 共通UI方針
- デザインシステム／コンポーネントライブラリ：
- レスポンシブ対応 (ブレークポイント／対応端末)：
- アクセシビリティ基準 (WCAG 等)：
- 多言語・タイムゾーン・通貨表記：

### 5.4 画面定義
画面ごとに 5.4.a〜k を埋める。**該当しない項目は「N/A — 理由」を 1 行で明記して残す。** 空欄にはしない。

#### 5.4.x [画面ID] 画面名

##### a) 概要
- 目的：
- 利用シーン (いつ／どの業務フローで開かれるか)：
- 対応機能ID：
- 利用者ロール：

##### b) 画面構成 (領域分割)
画面を意味的な領域に分割する。レイアウトの厳密な座標ではなく、論理ブロックを列挙する。

| 領域ID | 領域名 (例: ヘッダ／ナビ／検索フォーム／結果一覧／操作パネル／フッタ) | 役割 | 含むコンポーネントID |
| --- | --- | --- | --- |

##### c) コンポーネント一覧
表示・入力・操作の各 UI 部品を列挙する。共通部品は名前のみ書き、カスタム部品は概要も書く。

| コンポーネントID | 種別 (テキスト/ラベル/フォーム/テーブル/ボタン/モーダル/トースト等) | ラベル | 概要・特記事項 |
| --- | --- | --- | --- |

##### d) 入出力項目定義
入力フォームと表示フィールドをまとめて列挙する。

| 項目ID | 項目名 | 型 | 桁 | 必須 | 初期値 | 入力チェック (範囲・正規表現) | 表示条件 | 編集条件 (disabled/read-only) | 備考 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

##### e) アクション・イベント
ボタン・リンク・入力イベントごとに、契機→検証→処理→結果遷移を確定する。

| アクションID | 契機イベント (click / submit / onBlur 等) | 前提条件 | クライアント側検証 | 処理概要 (呼出 API/関数) | 成功時挙動 (遷移／更新／メッセージ) | 失敗時挙動 |
| --- | --- | --- | --- | --- | --- | --- |

##### f) 画面状態
画面が取り得る状態を網羅する。状態ごとに、表示する領域・コンポーネントの差分を書く。

| 状態 | 発生条件 | 表示内容の差分 |
| --- | --- | --- |
| 初期表示 | | |
| ローディング中 | | |
| データなし (empty) | | |
| エラー | | |
| 完了／成功 | | |

##### g) 権限・可視性 (アクセス制御)
| ロール | 閲覧可否 | 編集可否 | 操作可能アクション |
| --- | --- | --- | --- |

##### h) メッセージ一覧 (情報・警告・エラー)
| メッセージID | 種別 | 文言 | 表示契機 | 表示位置 (トースト/モーダル/インライン) |
| --- | --- | --- | --- | --- |

##### i) アクセシビリティ要件
- キーボード操作・タブ順序：
- フォーカス制御 (モーダル開閉時の戻り先など)：
- スクリーンリーダー向け ARIA 属性・代替テキスト：
- カラーコントラスト・フォントサイズ：

##### j) 画面間連携 (パラメータ受け渡し)
- 遷移元と受け取るパラメータ：
- 遷移先と渡すパラメータ：
- 「戻る」「キャンセル」「未保存変更あり」時の挙動：

##### k) レイアウト概念図 (任意)
ワイヤーフレーム画像／Figma リンク／Mermaid `block-beta` 等を **付録 C** に置き、ここはリンクのみ記載する。本セクション内に ASCII アートを書かない。

---

## 6. 帳票設計
### 6.1 帳票一覧
| 帳票ID | 帳票名 | 出力媒体 | 出力契機 | 頻度 |
| --- | --- | --- | --- | --- |

### 6.2 帳票レイアウト
### 6.3 帳票項目定義
| 項目名 | 型 | 桁 | 編集仕様 | 備考 |
| --- | --- | --- | --- | --- |

---

## 7. バッチ設計
### 7.1 バッチ一覧
| バッチID | バッチ名 | 概要 | 実行契機 | 実行時間帯 |
| --- | --- | --- | --- | --- |

### 7.2 バッチフロー
### 7.3 バッチ詳細
- 入力：
- 処理概要：
- 出力：
- 異常時リカバリ：

---

## 8. 外部インターフェース設計
### 8.1 外部IF一覧
| I/F ID | 連携先 | 方式 (REST/SFTP/DB等) | 方向 | 頻度 |
| --- | --- | --- | --- | --- |

### 8.2 インターフェース詳細
- 通信方式・プロトコル：
- 認証方式：
- データフォーマット：
- 項目定義：
- エラー処理・リトライ：

---

## 9. データベース設計
### 9.1 ER図
### 9.2 テーブル一覧
| テーブルID | 論理名 | 物理名 | 概要 |
| --- | --- | --- | --- |

### 9.3 テーブル定義
#### 9.3.x テーブル名
| 列名 | 物理名 | 型 | 桁 | NULL | PK | FK | 初期値 | 備考 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |

### 9.4 CRUD図
| 機能ID ＼ テーブル | T1 | T2 | ... |
| --- | --- | --- | --- |

### 9.5 データ量見積・保存期間

---

## 10. 非機能設計
### 10.1 可用性設計
- 冗長化方式：
- 障害時の切替：
- RTO / RPO：

### 10.2 性能設計
- 性能目標：
- 性能確保方針（キャッシュ、DB設計、負荷分散）：

### 10.3 セキュリティ設計
- 認証・認可：
- 通信暗号化：
- データ暗号化：
- 監査ログ：
- 脆弱性対策：

### 10.4 運用・保守設計
- 監視項目：
- バックアップ／リストア：
- ログ設計：
- ジョブ運用：

### 10.5 移行設計
- 移行対象データ：
- 移行方式（一括／段階）：
- 移行ツール：
- リハーサル計画：

---

## 11. インフラ設計
### 11.1 システム構成（物理 / クラウド）
### 11.2 ネットワーク構成
### 11.3 サーバ / サービス一覧
| 名称 | 役割 | スペック / プラン | 台数 |
| --- | --- | --- | --- |

### 11.4 ストレージ設計
### 11.5 ミドルウェア構成

---

## 12. テスト方針
### 12.0 テスト戦略 tier (test-strategy)
<!-- 必須: `strict` / `pipeline` / `ui` のいずれか。既定は `strict`。
     `spec-coexist:implementing-from-spec` と `spec-coexist:revising` が
     TDD Iron Law の RED 観測単位を決定するために参照する。
     選定理由は 1〜3 文で明記する。詳細は
     implementing-from-spec/references/tdd-discipline.md §Test Strategy Tiers -->

- **test-strategy:** `strict`
- **選定理由:** 

### 12.1 テスト種別
- 単体テスト：
- 結合テスト：
- 総合テスト：
- 受入テスト：
- 性能・セキュリティテスト：

---

## 13. 変更対象ファイル一覧
<!-- 必須: 本設計が追加・変更を見込むファイル／ディレクトリをすべて列挙する。
     parallelizing-subsystem-work の独立性判定（他サブシステム・docs/main-* との競合検出）に使用される。 -->
- 

---

## 14. 課題・リスク
| ID | 内容 | 影響 | 対応方針 | 担当 | 状況 |
| --- | --- | --- | --- | --- | --- |

---

## 付録
- A. 参考資料
- B. 用語集
- C. 画面モックアップ
- D. ER図 / 構成図
